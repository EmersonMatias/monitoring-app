import axios from "axios"

/**
 * 
 * 
 * export async function getUserCheckpoints(userId: number){
    const res = (await axios.get(`http://localhost:4000/checkpoints/${userId}`)).data
 
    return res
}
 */